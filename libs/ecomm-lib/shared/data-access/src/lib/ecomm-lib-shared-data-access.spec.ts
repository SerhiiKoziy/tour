import { ecommLibSharedDataAccess } from './ecomm-lib-shared-data-access';

describe('ecommLibSharedDataAccess', () => {
  it('should work', () => {
    expect(ecommLibSharedDataAccess()).toEqual('ecomm-lib-shared-data-access');
  });
});
